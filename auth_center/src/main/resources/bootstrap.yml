server:
  port: 10001
  servlet:
    context-path: /auth
spring:
  application:
    name: anth_center
  profiles:
    active: dev
  # nacos 服务注册中心
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
# eureka 服务注册中心
#eureka:
#  client:
#    service-url:
#      defaultZone: http://39.100.194.182:12345/eureka/

